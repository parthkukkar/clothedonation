import React from 'react';
import {images} from "../../constants";
const SubHeading = (props) => (
  <div style={{marginBottom:'1rem'}}>
   <p className="p__cormorant" style={{color:'black'}}>{props.title} </p>


  </div>
);

export default SubHeading;
